tags:
  - currency

summary: Delete a single Currency as defined by the Id provided

parameters:
  - in: path
    name: id
    schema:
      type: string
      example: EUR
    required: true

responses:
  200:
    description: OK
  404:
    description: NOT FOUND
  500:
    description: INTERNAL SERVER ERROR