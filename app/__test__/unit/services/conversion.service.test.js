const chai = require('chai');
const sinon = require('sinon');
const conversionService = require('../../../src/services/conversion.service');

const expect = chai.expect;

describe('services/conversion.service.js', () => {

    describe('calculateExchangeRate()', () => {

        describe('Given that all parameters are correct', () => {
            it('should return an object containing the "from" amount, "to" conversion amount and the "reference" rate.', () => {
                
            });
        })
    });
});