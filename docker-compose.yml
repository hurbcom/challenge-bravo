version: "3"
services:
  mongodb:
    image: mongo:latest
    restart: "no"
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: development
      MONGO_INITDB_DATABASE: desafio-hurb
  