{
    "Logging": {
        "LogLevel": {
            "Default": "Warning",
            "System": "Warning",
            "Microsoft": "Warning"
        }
    },
    "connectionStrings": {
        //local database
        "localDb": "server=#{db_addr};user id=#{User};password=#{Pass};database=#{BD};Allow User Variables=True",
        "redis": "#{cache_addr}"
    },
    "externalURLs": {
        "cryptoCompare": "https://min-api.cryptocompare.com/"
    }
}
