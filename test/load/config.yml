config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 1000
  ensure:
    p99: 150
    maxErrorRate: 0

scenarios:
  - name: "Exchange"
    flow:
      - get:
          url: "/exchange?from=USD&to=BRL&amount=1"
