class Currency {
    name: string;

    baseValue: number;

    constructor(name: string, baseValue: number) {
      this.name = name;
      this.baseValue = baseValue;
    }
}

export default Currency;