version: '3'
services:
  web:
    build: .
    ports:
      - "8082:8082"
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017" 
    command: ["--auth"]
    volumes:
      - ./config/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
      