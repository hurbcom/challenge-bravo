config:
  target: "http://localhost:8000"
  phases:
    - duration: 1
      arrivalRate: 1000

scenarios:
  - name: "Convert"
    flow:
      - get:
          url: "/convert?from=USD&to=BRL&amount=2"
