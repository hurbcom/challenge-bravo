const {
  CRYPTOCOMPARE_URL,
  CRYPTOCOMPARE_RETRY_DELAY = 100,
  CRYPTOCOMPARE_RETRY_TIMES = 1,
  CRYPTOCOMPARE_TIMEOUT = 3000,
  EXCHANGERATESAPI_URL,
  EXCHANGERATESAPI_RETRY_DELAY = 100,
  EXCHANGERATESAPI_RETRY_TIMES = 1,
  EXCHANGERATESAPI_TIMEOUT = 3000,
  NODE_ENV = 'development',
  PORT = 3000,
} = process.env;

const env = {
  CRYPTOCOMPARE_URL,
  CRYPTOCOMPARE_RETRY_DELAY,
  CRYPTOCOMPARE_RETRY_TIMES,
  CRYPTOCOMPARE_TIMEOUT,
  EXCHANGERATESAPI_URL,
  EXCHANGERATESAPI_RETRY_DELAY,
  EXCHANGERATESAPI_RETRY_TIMES,
  EXCHANGERATESAPI_TIMEOUT,
  NODE_ENV,
  PORT,
};


module.exports = env;
