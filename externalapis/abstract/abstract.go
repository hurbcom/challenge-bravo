// Package abstract provides bindings to interect with the Abstract API.
package abstract

const (
	// BaseURL is the base URL for the abstract API
	BaseURL = "https://exchange-rates.abstractapi.com/v1"
)
