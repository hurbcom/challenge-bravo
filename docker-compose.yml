version: "3"

services:

  redis:
    image: redis
    container_name: challenge-bravo-cache
    command: redis-server --requirepass redispassword
    restart: unless-stopped
    ports:
      - "6379:6379"
    expose:
      - "6379"
    # networks:
    #   - redis
  node:
    image: "node:latest"
    user: "node"
    container_name: challenge-bravo-nodejs
    working_dir: /app
    restart: always
    volumes:
      - ./nodejs:/app
    ports:
      - "3000:3000"
    command: "npm run server"
    environment:
      MONGO_STRING: mongodb://mongouser:mongopassword@mongo:27017/challenge-bravo?authSource=admin&&socketTimeoutMS=300000
      REDIS_HOST: redis://redis
      REDIS_PASSWORD: redispassword
    links:
      - mongo
      - redis
    # depends_on:
    #   - redis
    # networks:
    #   - redis
    #   - mongo
  mongo:
    image: mongo
    container_name: challenge-bravo-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongouser
      MONGO_INITDB_ROOT_PASSWORD: mongopassword
    ports:
      - "27017:27017"
    volumes:
      - ./data/mongo:/data/db
    expose:
      - "27017"
    # networks:
    #   - mongo

#   php:
#     image: php:alpine
#     container_name: challenge-bravo-php
#     environment:
#       APP_NAME: Laravel
#       APP_ENV: local
#       APP_KEY: base64:+ex+GmnJwljvO/g9gAgktjqnpBPz4OzqyVu4LERZ1ns=
#       APP_DEBUG: true
#       APP_URL: http://localhost
#       LOG_CHANNEL: stack
#       LOG_DEPRECATIONS_CHANNEL: null
#       LOG_LEVEL: debug
#       BROADCAST_DRIVER: log
#       CACHE_DRIVER: file
#       FILESYSTEM_DRIVER: local
#       QUEUE_CONNECTION: sync
#       SESSION_DRIVER: file
#       SESSION_LIFETIME: 120
#       DB_CONNECTION: mongodb
#       DB_HOST: mongo
#       DB_USERNAME: mongouser
#       DB_DATABASE: challenge-bravo
#       DB_PASSWORD: mongopassword
#       MONGO_STRING: mongodb://mongouser:mongopassword@mongo:27017/challenge-bravo?authSource=admin&&socketTimeoutMS=300000
#       REDIS_HOST: redis
#       REDIS_PASSWORD: redispassword
#       REDIS_PORT: 6379
#     working_dir: /app
#     command: "php artisan serve --host=0.0.0.0 --port=3001"
#     build:
#         dockerfile: php.Dockerfile
#     ports:
#       - 3001:3001
#     volumes:
#       - ./php-laravel:/app
#     links:
#       - mongo
#       - redis

# networks:
#   redis:
#     driver: bridge
#   mongo:
#     driver: bridge